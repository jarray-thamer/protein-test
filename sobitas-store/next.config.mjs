/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "portotheme.com",
      "www.confidenthealth.com",
    ], // Add your Cloudinary domain here
  },
};

export default nextConfig;
